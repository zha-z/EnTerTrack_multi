BASE_CONFIG: ostrack_deit_tiny_b0_j0_partial_adapt_ep15
MODEL_ROLE: posthoc_j0_b0_partial_adapt

TRAIN:
  MULTIVIEW:
    ENABLED: true
  PCUM:
    # J0v2 uses the grouped loader through TRAIN.MULTIVIEW, while the model and
    # actor remain a no-PCUM flat-multiview control.
    USE_REAL_MULTIVIEW: false

TEST:
  SAVE_DIR: output/research_master_goal/lspca_v2/j0_control
  CHECKPOINT_NAME: ostrack_deit_tiny_b0_j0v2_lspca_control_ep15
